resource "discord_invite" "inv_3z453tvuUb" {
  channel_id = discord_text_channel.txt_ch424587.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_4NNVaFqgxs" {
  channel_id = discord_text_channel.txt_ch424587.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_CGUdZazC6a" {
  channel_id = discord_text_channel.txt_ch424587.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_CkzaAXfrQD" {
  channel_id = discord_text_channel.txt_ch424587.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_N9qGDX7k9U" {
  channel_id = discord_text_channel.txt_ch424587.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_X96FVdSCq2" {
  channel_id = discord_voice_channel.vc_ch894141.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_Zwy7qatRXf" {
  channel_id = discord_text_channel.txt_ch424587.id
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}

resource "discord_invite" "inv_jkPhuDbu5T" {
  channel_id = "895564067425648640"
  max_age    = 0
  max_uses   = 0
  temporary  = false

  lifecycle {
    ignore_changes = [
      channel_id,
      max_age,
      max_uses,
      temporary,
    ]
  }
}
